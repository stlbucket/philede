mutation CommitWorkingArtifact(
  $id: UUID!
) {
  commitWorkingArtifact(input: {
    _id: $id
  }) {
    artifact {
      id
      nodeId
      name
      ddl
      artifactType: artifactTypeByArtifactTypeId {
        id
        nodeId
        name
      }
    }
  }
}